function 函数名(参数，参数)
{
	内容
  return ‘d’;
}
arguments.length;检测该函数里面传递了几个参数
arguments[0]第一个参数

<body onload=“函数名”>页面加载的时候自动调用

new Date();返回系统当前时间
传递一个参数表示毫秒数，系统自动把毫秒转换为时间
"2016.1.1"传递一个日期
(年,月，日，时，分，秒)

Date.parse(参数)它会把参数返回为毫秒数，格式看帮助文档
Date.UTC(参数)返回UTC时间，格式看帮助文档
toString();返回字符串形式，还有等等形式查看帮助文档
