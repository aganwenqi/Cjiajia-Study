建立一个数组a[4]使也会建立一个指针来指向该数组的内存，而该内存通常都是int *a=&a[0],也就是数组第一个元素的地址
strcat(a,b)表示把两个字符型数组合并，并且把合并的值赋给a，且\0会自动调在最后，注意，a数组空间需要足够大
strcpy(a,b)表示把字符串b拷贝给a，并覆盖
strcmp(a,b)表示两个字符串比较，假如b数量大于a，那输出大于0的数，反正输出小于0的数，等于输出0
strupr(a)把括号中的字符串变成大写
strlwr(a)把括号中的字符串变小写
strlen(a)测试括号中字符串长度